name: Deploy to EC2

on:
  push:
    branches:
      - develop
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0  # 取得完整 Git history

    - name: Set up JDK 21
      uses: actions/setup-java@v3
      with:
        java-version: '21'
        distribution: 'temurin'

    - name: Verify Project Structure
      run: ls -al  # 確認 pom.xml 是否存在

    - name: Build JAR
      run: |
        cd eurekaservice  # 如果 pom.xml 在子目錄，先切換過去
        mvn clean package -DskipTests

    - name: Print Build Completion Message
      run: echo "🎉 Build completed successfully!"
