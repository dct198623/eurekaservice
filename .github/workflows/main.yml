name: Deploy to EC2

on:
  push:
    branches:
      - develop
  pull_request:
    branches:
      - develop
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3
      with:
        fetch-depth: 0  # 確保完整 Git history
    
    - name: Set up JDK 21
      uses: actions/setup-java@v3
      with:
        distribution: 'temurin'
        java-version: '21'

    - name: Verify Project Structure
      run: ls -al  # 確保 `pom.xml` 或 `build.gradle.kts` 存在

    - name: Grant execute permission to Gradle wrapper
      run: chmod +x gradlew

    - name: Build Project
      run: ./gradlew build  # Kotlin 預設使用 Gradle

    - name: Run Tests
      run: ./gradlew test  # 執行測試

    - name: Upload JAR Artifact
      uses: actions/upload-artifact@v4  # ✅ 更新為 v4
      with:
        name: spring-cloud-app-jar
        path: build/libs/*.jar  # 上傳生成的 JAR 檔案

    - name: Print Build Completion Message
      run: echo "🎉 Build and test completed successfully!"
